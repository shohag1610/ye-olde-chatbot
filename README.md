# Ye Olde Chatbot

**Objective:** Build a simple chatbot interface using a pretrained model; later adapt it to speak in Shakespearean tavern style.

## Task 01 — Project setup
This repository contains the initial project scaffold:

- simple Chatbot class (`chatbot.py`)
- tests (`test_chatbot.py`) using `pytest`
- basic entrypoint (`main.py`)

## Quickstart

1. Create & activate a virtual env:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate